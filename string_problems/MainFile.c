#include<stdio.h>
#include "StringCollections.h"
void main(){
    char s[500] = "A database is an organized collection of data, generally stored and accessed electronically from a computer system.";
    char s1[500] = "A database is an organized collection of data, generally stored and accessed electronically from a computer system.";
    char hash[100] = "#b1c735!%49@&d37*2(d40fcb8-0d+e0b8af39299";
    char cpy[500];


    //printf("\nENTER THE STRING : ");
    //gets(s);

    //print_reverse_order(s);
    //word_count(s);
    //compare_string(s,s1);
    //count_content(hash);
    //copy_string(hash,cpy);
    //count_vowel_consonant(s);
    //find_max_occurrence(s);
    //string_sort(hash);
    //extract_substring(hash,10,20);
    //is_substring("ABCDEF","ABD");
    //is_substring("ABCDEF","BCD");
    //case_change(hash);
    //find_appareance(s);
    //remove_special(s);
    //find_frequency(s);
    //concat_string(s,hash);
    //find_largest_smallest_word(s);
    //convert_to_uppercase(s);
    //convert_to_lowercase(s);
    //check_hex('F');
    //check_hex('Z');
    //is_upper('A');
    //is_upper('c');
    //replace_space(s);
    //count_punctuation(s);
    print_until_newline(s);
    //puts(hash);
}
